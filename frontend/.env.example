# Copy this to .env.local for local development
# On Vercel, set VITE_API_URL in the project environment variables

# Your Railway backend URL (leave blank for local dev — Vite proxy handles it)
VITE_API_URL=
